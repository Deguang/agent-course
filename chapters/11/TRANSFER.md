# Chapter 11 · 迁移题(超出本章)

先预测，写进 [`JOURNAL.md`](../../JOURNAL.md);想做就自己加测试。

## 迁移 1 · SSE 服务化(把 loop 事件推给前端)
把 Day 1 loop 的每步事件，通过 SSE 端点逐条 flush 给客户端;前端(或 curl)实时消费。
- 预测:Day 2 的 `accumulate` 在服务端还是客户端跑?哪些是可迁移核心、哪些是前端框架特有?

## 迁移 2 · 部署到 Serverless
把 agent 服务部署到一个 Serverless 平台(现查平台文档)。
- 预测:执行时长上限会怎么影响"长任务 agent"?哪些状态必须外置(接 Day 4 session + 本章 suspend/resume)?

## 迁移 3 · 并发写冲突的合并策略
Day 7 的 `parallel`:两个分支并发都改共享状态的同一个键，merge 该怎么定策略?
- 预测:不可变更新(各出增量)vs 就地改共享对象，哪个能避免数据竞争?冲突键该用"最后写赢"还是"显式合并"?

## 迁移 4 · 跨服务多 agent(A2A)
当不同 agent 跑在不同服务边界，它们之间通信需要标准协议。
- 预测:这和本地 `runGraph` 的节点调用有何本质不同?(网络失败、超时、幂等、消息格式)——这是分布式系统问题，不只是 agent 问题。
