# Chapter 01 · 学习日志

> 这是你的地盘,直接在下面写。想重来就 `git checkout chapters/01/LOG.md`。

发**过程**,不发成品。预测错了、卡住了,都记下来 —— 那才是习得的痕迹。

## ① 我的预测(动手前写)

1. loop 转几圈(若先后调两次工具,两问都答):
   (a) role 序列会变成什么样: model->loop->model->loop
   (b) `model.step()` 会被调用几次: 5次
2. 谁执行:那条 tool-result 是模型放进 history 的,还是 loop 放的:
   - loop
3. 靠什么配对(两问都答):
   (a) `c1` 是从哪来的: 注册的tools
   (b) loop 回填时乱编一个新 id,会出什么问题: 无法匹配抛出错误

## ② 踩坑 / 卡在哪

- 没有理解清楚这个题目的背景及过程，盲目取写代码

## ③ 讲回来(测试绿之后,用自己的话)

- 为什么执行工具的是 loop 而不是模型:
- 不把 tool-result 喂回去就再叫模型,会坏在哪:

## ④ 迁移题记录

-
