# Chapter 05 · 迁移题(超出本章)

先预测，写进 [`JOURNAL.md`](../../JOURNAL.md);想做就自己加测试。

## 迁移 1 · 接到"发请求前"

把 `projectWithinBudget` 接到 Day 2 的 `historyToWire` 之前:每次请求模型前，先把完整历史投影到预算内，再翻译成 wire。
- 预测:投影后，`assistant-tools` 和它的 `tool-result` 一定成对出现吗?(必须——否则模型看到悬空调用)

## 迁移 2 · compaction 追加进 session

把"丢弃的旧组"总结成一条摘要，**追加**进 Day 4 的 session(不删旧)。恢复时读最新摘要 + 后缀。
- 预测:摘要该带什么元信息才能"从这条之后接着投影"?(比如 firstKeptEntryId)
- 摘要本身要不要也进未来的投影预算?

## 迁移 3 · 反序完成的工具结果

两个并发工具的结果**反序**到达(c2 先于 c1)。按数组相邻位置切，会不会切出半组?
- 预测:为什么"按 callId 建立集合归属"比"按数组相邻"更稳?

## 迁移 4 · 安全余量与输出预留

把"窗口总量 → 留给历史的预算"这步做出来:扣掉 system、期望输出、安全余量。
- 预测:安全余量为什么必要?(估算不精确 + 模型有时多说)
