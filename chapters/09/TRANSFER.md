# Chapter 09 · 迁移题(超出本章)

先预测,写进 [`JOURNAL.md`](../../JOURNAL.md);想做就自己加测试。

## 迁移 1 · 接进 Day 1 loop
把 `guardedExecute` 插到 Day 1 loop 执行工具的地方,`BudgetGuard` 插到每轮 step。
- 预测:deny/待批返回的 isError 结果,模型下一轮看到后典型会怎么反应?

## 迁移 2 · 按参数裁决,不只按工具名
`bash` 工具:`ls` 该 allow,`rm -rf` 该 ask/deny。让 policy 看**参数**。
- 预测:光靠工具名够吗?为什么危险常藏在参数里?

## 迁移 3 · 审计日志
每次裁决(allow/ask/deny + 谁批的)留痕,可回溯(接 Day 4 的追加日志)。
- 预测:审计对"事后追责"和"注入取证"为什么关键?

## 迁移 4 · 注入红队
构造一个"工具返回里藏恶意指令"的场景,看你的 agent 会不会被带偏;再用"高危动作走 ask"挡住后果。
- 预测:哪一层(最小权限/数据非指令/人工审批)最先挡住它?
