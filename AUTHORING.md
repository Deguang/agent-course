# AUTHORING.md — 课程设计公约

**约束任何编写/修改章节与测验的人(含 AI)。** 这些规则不是凭空立的——每一条都来自真实踩过的坑(括号里是当初的缺陷)。写完/改完一章,按文末清单自检。

与另两份公约配合:[`AGENT.md`](./AGENT.md)(tutor 不过度接管)、[`CONTENT.md`](./CONTENT.md)(不教错、术语首次即解释、可运行验证)。

---

## 一、章节设计

### 1. 先建前提,再深入(别有前置缺口)
要求学员做某件事之前,先确认"做这件事所需的概念、术语、词汇"**都已在此之前建立**。
- 缺陷来源:第 1 章一上来就"写 agent loop",却没先讲"什么是 agent" → 学员懵。
- 缺陷来源:预测题直接用 `history`/`role`/`model.step()`,但从没介绍过这些词。
- **自检:**"要完成这一步,学员必须已经知道什么?这些在此之前出现并解释过吗?"

### 2. 教,不要复述规则;每个"做什么"配一个"为什么"
正文和 starter 注释,要帮学员理解"这是什么 / 为什么这么设计",**不是**重复学习契约或元规则。
starter 的分步思路里,**每一步"做什么"都要给出"为什么"**——只给操作步骤(how)会让学员照抄而不理解。
- 缺陷来源:`agent.ts` 注释只在重复契约("先预测""别抄"),学员看完仍"不知道要干什么"。
- 缺陷来源:runAgent 思路只列"先记提议、再执行、再继续"(how),没说为什么先记、为什么用原 id、为什么继续而非返回 → 学员只能照抄。
- **自检:**"这段是在教概念/讲理由,还是只在复述元规则或列操作步骤?"

### 3. 角色与概念一致
比喻/示例里的每个角色、每个术语,从头到尾指**同一个真实实体**,且与 demo、代码、测试一致。
- 缺陷来源:loop 比喻里"你"一会是用户、一会是执行者,和"执行是 loop 的事"这条主线自相矛盾;还漏了最初的用户请求。
- **自检:**通读比喻——每个角色是否只对应一个实体?和 demo/代码/测试对得上吗?第一步(用户请求)有没有漏?

### 4. 单一真源,别重复
概念/类型的定义只放一处(**通常是代码**),正文**指过去**而非重抄一遍。
- 缺陷来源:README 重抄了 `agent.ts` 里的类型说明 → 两处会漂移。改成"去读 `agent.ts` 顶部类型"。
- **自检:**"这段解释和别处(尤其代码)是否重复?能不能改成指向真源?"

### 5. 先观察,再动手
抽象概念先给一条**可运行的具体轨迹**(demo)让学员"看见",再要求实现。
- 缺陷来源:早期直接让写 `runAgent`,没有 demo → 概念太空。加了 `demo:01` 后才具体。
- **自检:**"学员在动手前,有没有一个能跑、能看见的具体例子?"

### 6. 由浅入深的入口坡度
每门课/每章要有"定向 → 观察 → 预测 → 动手 → 讲回来 → 迁移"的坡度,别让学员从最陡处起步。
- 缺陷来源:没有 Ch00 定向,直接 Ch01 动手。补了 Ch00(什么是 agent)。

---

## 二、预测题 / 测验设计

### 1. 真预测,不是抄写
答案**不能**是刚展示过的内容的直接复制。好预测需要**推理**,且**有可能答错**。
- 缺陷来源:Q1 问"role 顺序",但 demo 刚把顺序印出来了 → 是"抄",不是"测"。
- **自检:**"学员能不能从刚读的内容里直接抄出答案?能 = 废题,重写。"

### 2. 每题测一个不同维度
题目之间不重叠,各测一个概念/不变量。
- 缺陷来源:Q1、Q2 都在问"`model.step()` 调几次" → 重复。改成:转几圈 / 谁执行 / 靠 id 配对,三个维度。
- **自检:**"两题的答案是否依赖同一个理解点?是 = 合并或替换。"

### 3. 用学员已有的词汇提问
提问所用术语必须已经建立(承接章节规则 1)。
- **自检:**"这题里每个术语,学员此刻都认识吗?"

### 4. 勾住本章的不变量 / 测试
好预测题应对应**测试实际检查的不变量**——答对预测 = 已想清实现骨架。
- 正例:三题分别对应 loop 终止、执行归属、callId 配对,都是测试会验的。

### 5. 推广题优先于复述题
"如果情况变成 N 次会怎样"这类**推广题**,比"复述单次轨迹"更能区分"看过"和"懂了"。
- 缺陷来源:把 Q1 从"复述单次 role 顺序"改成"先后调两次工具会怎样",立刻变成真检验。

### 6. 措辞不留歧义
一题若有多个分镜,用 `(a)/(b)` 明确列出并写"两问都答";**别用 `/` 或 `、`** 连接子问——学员分不清是"或(答一个)"还是"和(都答)"。
- 缺陷来源:模板写成"c1 从哪来 / 乱编 id 会怎样",学员问"斜杠是一个问题两分镜还是两步骤?"。

---

## 三、工程卫生(影响阅读体验)

- **仓内引用用相对链接** `[x](./x)`,VS Code 才解析为内部文件(可点开),不被当外链;别留裸文件名。(缺陷:`LOG.md` 裸名/被当外链)
- **不留废弃残留文件**,尤其别误提交进 git。(缺陷:废弃方案的 `LOG.local.md` 被提交)
- 每章 `README` 结构统一;`index.html` 是预览,内容以各章 `README.md` 为准。

---

## 作者自检清单(写完/改完一章,逐条过)

- [ ] 本章要求的每个动作,前提(概念/术语/词汇)都在之前建立了吗?(§一.1)
- [ ] 正文/注释在教概念,而不是复述规则?(§一.2)
- [ ] 比喻/示例的角色一致、且和 demo/代码/测试对得上?(§一.3)
- [ ] 有没有和代码重复的解释?能否改为指向真源?(§一.4)
- [ ] 抽象概念前,有没有可运行的 demo?(§一.5)
- [ ] 每道预测题:学员能直接抄答案吗?(不能才合格,§二.1)
- [ ] 题目之间是否各测一维、不重叠?(§二.2)
- [ ] 题目用的术语学员都认识吗?(§二.3)
- [ ] 预测题勾住了本章测试的不变量吗?(§二.4)
- [ ] 仓内链接是相对链接、无裸名、无残留文件?(§三)
- [ ] 通用概念对齐主流、API 现查实跑、术语首次即解释?(见 `CONTENT.md`)
