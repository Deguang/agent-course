# 复制为 .env 后填入,任选其一即可(.env 已被 git 忽略,不会提交)。
#   cp .env.example .env
#
# 用法:npm run live:02 —— 见 chapters/02/live.ts 的 pickProvider()。
# 按下面顺序探测:哪个 key 在环境里就用哪个,靠上者优先。
# 全部走 OpenAI 兼容端点,换 provider 只改 key、不动核心 loop。

# ── 1. GLM(智谱)· 国内免费首选 ──────────────────────────
# glm-4-flash 免费、国内直连、支持工具调用。申请:https://open.bigmodel.cn
GLM_API_KEY=
# 兼容别名(设了 GLM_API_KEY 就不必再设这个)
# ZHIPU_API_KEY=

# ── 2. Groq · 免费额度、极快 ─────────────────────────────
# 模型 llama-3.3-70b-versatile。申请:https://console.groq.com/keys
# GROQ_API_KEY=

# ── 3. Google Gemini · 有免费层 ──────────────────────────
# 模型 gemini-2.0-flash。申请:https://aistudio.google.com/apikey
# GEMINI_API_KEY=
# 兼容别名
# GOOGLE_API_KEY=

# ── 4. OpenRouter · 聚合多家、部分模型免费 ───────────────
# 模型 meta-llama/llama-3.3-70b-instruct。申请:https://openrouter.ai/keys
# OPENROUTER_API_KEY=

# ── 5. Ollama 本地 · 零 key、完全离线 ────────────────────
# 装 ollama 后 `ollama pull qwen2.5`,设模型名即启用(端点 localhost:11434)。
# OLLAMA_MODEL=qwen2.5
